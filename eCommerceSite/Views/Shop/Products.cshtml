
@using eCommerceSite.Data;
@using eCommerceSite.Models;
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="d-flex flex-row h-100">

    <div class="offcanvas offcanvas-start show category-offcanvas h-100" data-bs-keyboard="false" tabindex="-1" data-bs-scroll="true" data-bs-backdrop="false" id="categories" aria-labelledby="categoriesLabel">
        <div class="offcanvas-header">
            <h5 class="offcanvas-title" id="categories">Categories</h5>
        </div>
        <div class="offcanvas-body">
            @{
                foreach (string name in Enum.GetNames(typeof(Categories)))
                {
                    <a href="@Url.Action("Products", "Shop", new {id = name})">@name</a>
                    <br />
                }
            }
        </div>
    </div>


    <div class="shop-container container d-flex flex-column overflow-auto p-4">
        <div class="row row-cols-3 m-0">
            @foreach (Product product in Model)
            {
                string img = (product.Image is not null) ? product.Image : "../../images/product.png";
                <div class="p-3">
                    <div class="card">
                        <img class="card-img-top" src=@img alt="Product Image"/>
                        <div class="card-body">
                            <h5 class="card-title">@product.Name</h5>
                            <p class="card-text">@product.Price.ToString("C", System.Globalization.CultureInfo.CurrentCulture)</p>
                            <button class="btn btn-light">Purchase</button>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
